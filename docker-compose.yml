x-asm: &asm
  image: ghcr.io/vinicius-piassa/rinha-backend-2026-asm:fc8535e
  platform: linux/amd64
  restart: unless-stopped
  ulimits:
    memlock: -1
    nofile:
      soft: 65535
      hard: 65535

services:
  api1:
    <<: *asm
    command:
      - /asm_server
      - /sockets/api1.sock
    working_dir: /
    volumes:
      - sockets:/sockets
    deploy:
      resources:
        limits:
          cpus: "0.475"
          memory: "171MB"

  api2:
    <<: *asm
    command:
      - /asm_server
      - /sockets/api2.sock
    working_dir: /
    volumes:
      - sockets:/sockets
    deploy:
      resources:
        limits:
          cpus: "0.475"
          memory: "171MB"

  lb:
    <<: *asm
    command:
      - /asm_lb
      - "9999"
      - /sockets/api1.sock
      - /sockets/api2.sock
    ports:
      - "9999:9999"
    volumes:
      - sockets:/sockets
    depends_on:
      - api1
      - api2
    deploy:
      resources:
        limits:
          cpus: "0.05"
          memory: "8MB"

volumes:
  sockets:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: size=4m,mode=0777
